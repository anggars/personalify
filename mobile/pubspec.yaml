name: personalify
description: "Personalify - Your Spotify music personality dashboard"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0-0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  flutter_web_auth_2: ^5.0.0
  flutter_secure_storage: ^9.0.0
  cached_network_image: ^3.3.1
  provider: ^6.1.2
  google_fonts: ^6.2.1
  fl_chart: ^0.69.0
  shimmer: ^3.0.0
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
